[build]
builder = "DOCKERFILE"
dockerfilePath = "frontend/Dockerfile.prod"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
# No startCommand needed - it's in the Dockerfile